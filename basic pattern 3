#include<stdio.h>
int main()
{
    int i,n,j;
    printf("Enter number of rows :");
    scanf("%d",&n);
    for(i=n;i>=0;i--){
      for(j=i;j<n;j++){
      printf("%c",'A'+j);
    }
      printf("\n");
  }
}
