#include<stdio.h>
int main()
{
      int i,num;
      long long fact=1;
      printf("Enter number : ");
      scanf("%d",&num);
      for(i=num;i>=1;i--)
      {
          fact = fact*i;

    }
    printf("the factorial of %d is: %d",num,fact);
}
