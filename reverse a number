#include<stdio.h>
int main()
{
    int i,num,rem,temp,rev=0;
    printf("Enter number : ");
    scanf("%d",&num);
    temp = num;
    while(num){
        rem = num%10;
        num = num/10;
        rev = rev*10+rem;

   }
   printf("the reverse of %d is: %d",temp,rev);
}
